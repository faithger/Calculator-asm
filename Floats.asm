; Data structure to store 64 bit long positive and negative decimal numbers!
; 1 Signing bit for positive and negative
; 11 Exponent bits
; 52 Mantissa bits
; The numbers are built like this: Asumed 1.(Mantissa bits) * 2^Exponents bits numberd

SECTION .data

SECTION .text

global _start

_start:
    
